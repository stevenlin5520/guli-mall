server:
  port: 10300


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://39.100.128.196:3306/gulimall_ums
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: mall-member
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml

